meta {
  name: Crear componente
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/componentes
  body: json
  auth: inherit
}

body:json {
  {
    "recursoId": 2,
    "cantidad": 48
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
