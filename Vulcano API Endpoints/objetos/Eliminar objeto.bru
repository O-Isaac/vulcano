meta {
  name: Eliminar objeto
  type: http
  seq: 5
}

delete {
  url: http://localhost:8080/api/objetos/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
