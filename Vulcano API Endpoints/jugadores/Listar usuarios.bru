meta {
  name: Listar usuarios
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/jugadores
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
