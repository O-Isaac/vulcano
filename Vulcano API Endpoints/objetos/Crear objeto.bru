meta {
  name: Crear objeto
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/objetos
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "nombre": "Excalibur Prime",
    "desc": "Aquel que lucho en la caida del imperio orokin, no deberias tener este objeto su plano fue perdido"
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
