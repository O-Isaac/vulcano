meta {
  name: Actualizar objeto
  type: http
  seq: 4
}

put {
  url: http://localhost:8080/api/objetos/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "nombre": "Excalibur Umbra"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
